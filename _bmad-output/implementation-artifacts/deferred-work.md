# Deferred Work

Cross-story backlog of review findings intentionally deferred. Each entry lists the source review, original severity, and the reason for deferral so future sessions can re-evaluate.

**CI cadence convention (2026-04-24):** `.github/workflows/ci.yml` declares `paths-ignore: ['_bmad/**', '_bmad-output/**']` on both `push` and `pull_request` triggers. Commits touching ONLY those paths do not trigger CI — meant for BMad bookkeeping (status flips, review notes, deferred-work entries). Any commit touching code, manifests, workflow files, or other artifacts falls through paths-ignore and triggers the full 4-job matrix.

## Deferred from: code review of 1-1-bootstrap-cargo-project-with-hand-authored-cargo-toml (2026-04-22)

- **`[package].rust-version` MSRV field not set** — Bevy 0.18 / `bevy_egui` 0.39 require rustc 1.89. Deferred to Story 1.3 (rust-toolchain.toml authoring) where MSRV metadata naturally lives.
- **`[profile.dev.build-override] opt-level = 0` not added** — Bevy's fully-recommended dev-profile snippet includes both `build-override` and `package."*"`; only the latter is in the current skeleton. Small compile-speed win; revisit in Story 1.3 toolchain polish or at an M4/M6/M9 upgrade window. **Re-deferred by Story 1.3 (2026-04-23)** to the M4 upgrade window: the compile-speed delta is imperceptible at the current single-`fn-main` codebase size, and bundling `Cargo.toml` profile changes with the next Bevy-version bump keeps manifest churn concentrated at governance-approved windows.
- **`default-features = false` may omit winit/wgpu-Metal on Windows/macOS** — Bevy 0.18's `"3d"` feature collection should transitively pull the needed renderer/windowing crates; this will be empirically validated when Story 1.5 opens the first window on each platform. If Story 1.5 surfaces a missing-feature error, patch back into Cargo.toml.
- **No explicit `[[bin]] name = "asteroids3d"` (lowercase)** — Binary inherits mixed-case package name (`asteroids3D`). Spec explicitly routes release-binary naming to Story 4.10 / Epic 10 packaging work. Leave as-is for now.

## Deferred from: code review of 1-3-toolchain-lint-and-format-configuration (2026-04-23)

- **MSRV `1.89` declared in `Cargo.toml` but never exercised by CI** — `rust-toolchain.toml` pins everyone to `1.94.1`. Promise to downstream consumers that the project builds on 1.89 is unverified. **Resolution path:** add a "msrv-check" job to Story 1.4's CI matrix running `cargo +1.89 check` on a single platform (Linux suffices). Low-severity today because no downstream consumer exists; becomes real the moment we publish to crates.io or accept external contributions.
- **`project: asteroids3D` typo (pre-existing) in `sprint-status.yaml`** — fixed in `Cargo.toml` by commit `113eebe` but still misspelled in all BMad artifacts (`sprint-status.yaml`, `architecture.md`, `prd.md`, `epic-1-*.md`, …). **Resolution path:** dedicated chore story "fix project-name typo across planning + bookkeeping artifacts" — rename wave, no code impact. Not 1.3 scope (1.3 only touches toolchain/lint/format).

  > **✅ RESOLVED 2026-04-24 by typo-sweep chore** — commit `c2f1327` (`chore: fix asteriods3D → asteroids3D typo across all artifacts`): 24 files changed, 129 replacements, 1 file renamed (`1-7-splash-screen-shows-asteriods3d-and-transitions-to-mainmenu.md` → `-asteroids3d-`). Scope: code (`src/splash.rs` SPLASH_TEXT), config (`_bmad/bmm/config.yaml` project_name), planning artifacts (architecture, prd, all epic files, epics index/list/overview, requirements inventory, readiness report), implementation artifacts (stories 1.1/1.3/1.4/1.5/1.7, sprint-status.yaml, deferred-work.md). Verification: `grep -rn 'asteriods' .` → 0, local sweep (check/build/test/clippy/fmt) clean, runtime verified on macOS (`SPLASH_TEXT = "asteroids3D"` renders correctly), CI run `24883472863` all 4 jobs green, 0 warnings/errors across full log. Story 1.7 Dev Notes § "Typo preservation" called out this chore as the natural landing point — honored.
- **`rust-toolchain.toml` lacks a `targets = [...]` field** — fine for one-native-runner-per-OS CI, breaks the moment anyone attempts cross-compilation. **Resolution path:** add to Story 1.4's Definition of Done — either (a) confirm CI uses native targets only, or (b) amend `rust-toolchain.toml` with explicit `targets`. No code change in 1.3.

## Deferred from: code review of 1-4-three-platform-ci-matrix (2026-04-23)

- **Third-party action pinning — SHA-pin vs tag/branch** — `dtolnay/rust-toolchain@master` (branch!) and `actions/checkout@v4` (moving major tag). GitHub security guide recommends full-SHA pinning for third-party actions. Revisit at M4/M6 upgrade window when Steam/Apple secrets may land in CI. Sources: Blind Hunter + Edge Case Hunter.
- **MSRV job lacks `--all-targets`** — `cargo check` without `--all-targets` skips tests/examples/benches on the Rust 1.89 leg. No-op today (zero tests); becomes real when Story 1.5+ adds tests. Fix: add `--all-targets` to `cargo check (MSRV)` step. Surface in the first test-adding story.
- **MSRV version hardcoded in `ci.yml` (two places); no dynamic sync with `Cargo.toml:5`** — If `rust-version` in `Cargo.toml` is bumped without editing `ci.yml`, MSRV job silently tests the obsolete pin. Fix options: (a) enforce dual-edit via upgrade-window checklist, (b) read MSRV dynamically via `cargo metadata`. MVP has ~3 MSRV bumps planned; option (a) is low-ceremony.
- **`cancel-in-progress: true` cancels in-flight `master` runs on rapid back-to-back pushes** — `.github/workflows/ci.yml:12`. Spec stated incorrectly that master has "unique refs" — all master pushes share `refs/heads/master`. Fix: condition on `github.ref != 'refs/heads/master'`. Low impact for solo-dev push cadence.
- **Same-repo PR double CI runs (push + pull_request events do not coalesce)** — `.github/workflows/ci.yml:6-13`. With both `push: branches: ["**"]` and `pull_request:`, a same-repo PR triggers two runs with different `github.ref` values (`refs/heads/feature` vs `refs/pull/N/merge`). Fix: restrict `push:` to `[master]` and let `pull_request:` carry feature-branch CI. Edge Case Hunter rated HIGH; Till-impact MED (rare PR flow in solo dev).
- **No `tags:` filter + future `release.yml` concurrency interaction risk** — `.github/workflows/ci.yml:7-8`. When Story 4.10 adds tag-triggered releases, concurrency won't coalesce. Surface in Story 4.10's design; not today's problem.
- **No retry wrapper around transient `apt-get update` failures** — `.github/workflows/ci.yml:34,77`. GitHub runner mirrors 503 occasionally; `&&` short-circuits → flaky red legs for non-code reasons. Mitigation: `for i in 1 2 3; do sudo apt-get update -y && break; sleep 5; done`.
- **Linux apt-deps duplicated across `build` and `msrv-check` jobs** — `.github/workflows/ci.yml:30-41,73-83`. Any dep addition must land in two places; drift risk. Fix: extract to composite action `.github/actions/install-linux-deps/` or YAML anchor. Acceptable for 2 copies; revisit at 3+ jobs.
- **`.gitattributes` lacks explicit binary markers for incoming assets** — `.gitattributes:2`. `* text=auto eol=lf` relies on Git's heuristic for `.png`, `.blend`, `.gltf`, `.ogg` (land in Story 2.1+). Pre-declare `*.png binary` etc. before first asset commit to eliminate heuristic risk.
- **`--workspace` not on clippy; future workspace-split gap** — `.github/workflows/ci.yml:59`. Single-crate today; a workspace split (architecture.md:130-131 permits post-M3) would make `clippy --all-targets` lint only the root crate. Surface when workspace split is considered.

## Review correction (2026-04-22) — formerly dismissed finding now confirmed real

> **✅ RESOLVED 2026-04-23 by Story 1.5** — removed `bevy_egui` and its `[target.'cfg(debug_assertions)'.dependencies]` host block from `Cargo.toml` (commit `03eb7a4`). Manifest warning cross-platform eliminated: CI run `24842252974` full-log grep → 0 `warning:` / 0 `error:` across 3542 lines (vs. 3× in Story 1.4's logs). Re-introduction planned at the M2 debug-panels story as an optional feature-flag dep (`[features] dev-tools = ["dep:bevy_egui"]` with `optional = true`), per `docs/plugin-compatibility.md` → Deferred / Planned re-introduction. Historical context preserved below for audit trail.

- **🐛 `[target.'cfg(debug_assertions)'.dependencies]` does NOT strip `bevy_egui` from release builds** — Cargo emits this warning on `cargo check`:
  `warning: Found 'debug_assertions' in target.'cfg(...)'.dependencies. This value is not supported for selecting dependencies and will not work as expected.`
  Cargo evaluates `cfg()` predicates in dependency tables BEFORE rustc-flag processing, so `debug_assertions` is treated as always-true and `bevy_egui` is pulled into every build (debug AND release). The Blind Hunter and Edge Case Hunter both raised this in the original 2026-04-22 review; both findings were incorrectly dismissed under the assumption that the cargo compile success implied correct semantics. The compile DID succeed — but the design intent ("dev-only egui, stripped from release") is silently broken.
  **Source of error:** the `Cargo.toml Skeleton` in story 1.1's Dev Notes (and presumably the upstream `architecture.md`) prescribes this exact (broken) pattern. Architecture doc may need an erratum.
  **Candidate fixes (architectural, NOT a Story 1.1 micro-patch):**
  1. Move `bevy_egui` to `[features] dev-tools = ["dep:bevy_egui"]` and run dev with `cargo run --features dev-tools`. Clean, explicit, but requires a features-section convention.
  2. Move `bevy_egui` to `[dev-dependencies]`. Only available in tests/examples — does NOT solve the "I want it in `cargo run` debug" use case.
  3. Keep `bevy_egui` as an unconditional dep, gate USAGE in Rust code with `#[cfg(debug_assertions)]`. Egui still in Cargo.lock and compile graph for release (binary bloat persists), but at least no runtime overhead.
  Recommended: option 1 (feature flag) — but defer to Story 1.5 (first time the egui plugin actually gets registered) so the fix and the first usage land together.

## Deferred from: code review of 1-5-minimal-bevy-app-opens-a-window-on-all-three-platforms (2026-04-23)

- **Historical doc-drift: planning artifacts still list `bevy_egui` as a current pinned dep** — hits in `_bmad-output/planning-artifacts/epics/epic-1-foundation-plugin-compatibility-gate.md:22,42`; `_bmad-output/planning-artifacts/epics.md:128,177,282,451,471`; `_bmad-output/planning-artifacts/epics/epic-list.md:5`; `_bmad-output/planning-artifacts/epics/requirements-inventory.md:108,157`. These are historical Story 1.1/1.2 ACs that were accurate at their time. **Resolution path:** dedicated doc-sync chore story (or append 2026-04-23 erratum pointers at the next planning-sweep story). Rewriting historical ACs in-place is rewriting history; a pointer-to-current-truth is safer. Source: code-review Edge Case Hunter EC3–EC6.
- **`architecture.md` prescribes the broken `cfg(debug_assertions)` pattern + shows `bevy_egui` in the starter Cargo.toml skeleton** — `_bmad-output/planning-artifacts/architecture.md:256,977`. Explicitly M2-owned per Story 1.5's Architecture Compliance section — the architecture erratum lands with the M2 debug-panels story alongside the first actual feature-flag usage. Flagged so a future M2 implementer does not copy the broken pattern. Source: code-review Edge Case Hunter EC7.
- **Story 1.2 artifact still shows `bevy_egui` in its gated-plugins matrix + `cfg(debug_assertions)` Known Issue** — `_bmad-output/implementation-artifacts/1-2-plugin-compatibility-verification-gate.md:72,113,123,156,210`. **Resolution path:** append a 2026-04-23 addendum noting removal by Story 1.5; leave historical body intact. Source: code-review Edge Case Hunter EC8.
- **`App::run()` return value (`AppExit`) is discarded by `fn main()` in `src/main.rs:7`** — Bevy 0.18's `App::run() -> AppExit` carries a crash-signal (`AppExit::Error(_)`) that is silently swallowed when `fn main()` returns `()`. Clippy did not flag it under current config. **Resolution path:** land the `fn main() -> AppExit { App::new()…run() }` pattern when `main.rs` is next touched — natural landing point is Story 1.6 (GameState wiring). Source: code-review Blind Hunter BH14 + Edge Case Hunter EC1.

  > **✅ RESOLVED 2026-04-24 by Story 1.6** — `fn main() -> AppExit` landed in `src/main.rs` (commit `19ed03c`) alongside the GameState wiring. `App::run()`'s return value is now propagated via the missing trailing `;`. CI run `24880100462` full-log grep: 0 `warning:` / 0 `error:` across 2059 lines on 3 OSes + MSRV. Bundled with 1.6 per deferred-work.md's explicit assignment (natural landing point: next story touching `main.rs`).
- **`.claude/scheduled_tasks.lock` runtime artifact is not gitignored** — appeared in the working tree during a Claude Code `ScheduleWakeup` call. `.gitignore` excludes `.claude/settings.local.json` (same category) but not arbitrary runtime state. **Resolution path:** add `.claude/*.lock` (or the specific filename) at the next `.gitignore`-touching story. Story 1.5's Task 7 scope guardrails explicitly forbid touching `.gitignore`. Source: code-review / dev-story verification.

## Deferred from: code review of 1-6-gamestate-enum-with-bevy-states-skeleton (2026-04-24)

- **`bevy_winit` emits `WARN Skipped event Destroyed for unknown winit Window Id` on window close** — reliably observed on macOS during `cargo run` close in Story 1.6 (`/tmp/story-1-6-run.log`). Known Bevy 0.18 winit-event race between `WindowCloseRequested` and `Destroyed`; not Story-1.6-introduced and not reproducible in CI (no interactive close). **Resolution path:** re-evaluate at Bevy version-bump window (M4). If still present post-bump, file upstream issue or gate via custom `terminal_ctrl_c_handler` replacement. Flagged now so future story Debug Logs don't treat this WARN as a regression signal. Source: code-review light-pass LOW-1 (Story 1.6).

## Deferred from: code review of 1-7-splash-screen-shows-asteroids3d-and-transitions-to-mainmenu (2026-04-24)

- **`SplashConfig.timer` latches after first fire; no re-entry reset path** — `TimerMode::Once` stays at "finished" after `just_finished()` fires once. If a future story re-enters `GameState::Loading` (e.g., restart-to-loading on error, dev hot-reload, or a meta-currency-earned-cutscene flow), `tick_splash_timer` would never re-fire `just_finished()` → state machine hangs in Loading. **No current re-entry path exists** in M0/M1 (the flow is unidirectional Loading → MainMenu), so not a Story 1.7 regression. **Resolution path:** the first future story introducing a Loading-re-entry path must either (a) add a reset system on `OnEnter(GameState::Loading)` that does `config.timer.reset()`, or (b) recreate the SplashConfig resource on entry. Flagged now so the land-mine is visible to that story's author. Source: code-review light-pass LOW defer (Story 1.7). [src/splash.rs:12-21]

## Deferred from: code review of 2-1-visualplugin-skeleton-reference-scene (2026-04-27)

- **`spawn_reference_scene` is not idempotent on `OnEnter(Loading)` re-entry** — `src/visual/reference_scene.rs:12-16`. The system is bound to `OnEnter(GameState::Loading)` with no idempotency guard and no cleanup on `OnExit(Loading)` (intentional — the reference scene must persist into MainMenu/Arena per the story spec). If a future story adds a "return to splash / restart-to-loading" path that re-enters `GameState::Loading`, every entity (Camera3d + 3 meshes + 3 PointLights = 7 entities + 6 asset handles) is duplicated per re-entry, leaking entities and producing multiple Camera3d at the same `order: -1` (Bevy 0.18 warns at runtime on ambiguous render order). **No current re-entry path exists** in M0/M1 — the splash flow is unidirectional `Loading → MainMenu`, mirroring the same constraint that justified Story 1.7's `SplashConfig.timer` deferral. **Resolution path:** the first future story introducing a Loading-re-entry path must either (a) add a despawn-by-`ReferenceSceneEntity` query at the top of `spawn_reference_scene`, (b) gate spawn on a `Local<bool>` once-flag, or (c) move scene setup to `OnEnter(SomeRunOnceState)` if a more specific state lifecycle emerges. Source: code-review light-pass Edge Case Hunter finding #3 (Story 2.1). [src/visual/reference_scene.rs:12-16]

## Observed during: 2-1-visualplugin-skeleton-reference-scene dev verification (2026-04-27)

- **`splash::cleanup_loading_entities` cleanup-iteration race exposed (NOT introduced) by Story 2.1** — `src/splash.rs:67-73`. The cleanup query returns 3 entities tagged `LoadingStateEntity` (Camera2d, parent Node, child Text — child was tagged by Story 1.7's defensive review patch). Bevy 0.18's `ChildOf` linked-spawn semantics auto-despawn children when the parent is despawned; if archetype-iteration order despawns the parent Node before the child Text, the child is auto-removed by linked-despawn, and the iterator's subsequent direct despawn on the (now invalid) child entity emits `WARN bevy_ecs::error::handler: Entity despawned: The entity with ID Nv0 is invalid; its index now has generation 1.` Story 2.1's added entity set (Camera3d + 3 meshes + 3 lights, all tagged `ReferenceSceneEntity`) shifts archetype iteration order enough to expose the race that Story 1.8's narrower entity layout happened to avoid. **Behavior is unaffected** — splash transition completes, `entered MainMenu` lifecycle log fires, no panic, no missed despawn. WARN is cosmetic/diagnostic only. **Resolution path:** at the next story that touches `src/splash.rs` (likely Story 3.1 title-screen MainMenu UI), apply one of: (a) replace `commands.entity(entity).despawn()` with `commands.entity(entity).try_despawn()` per the WARN message's own suggestion (`queue_handled` / `queue_silenced` are the modern aliases); (b) remove the now-redundant `LoadingStateEntity` marker from the child Text spawn — linked-despawn handles it via the parent Node, and Story 1.7's defensive-marker patch is actively counter-productive in Bevy 0.18's hierarchy semantics. Source: 2-1 dev-story verification log (`/tmp/story-2-1-run.log`). [src/splash.rs:42-50, 66-73]

## Deferred from: code review of 1-8-tracing-based-logging-with-panic-hook-to-log-file (2026-04-27)

- **Shared `Arc<Mutex<File>>` between fmt-layer and panic hook** — `src/logging.rs:67,96`. Panic hook opens its own `OpenOptions::append` handle while the fmt file layer holds a separate `Mutex<File>`. Two independent OS file handles writing concurrently can interleave bytes (panic backtrace mid-line with tracing output, especially on Linux for writes >`PIPE_BUF`), and on Windows the second `open` may fail due to share-mode. Currently single-threaded panic source (Bevy main thread); becomes real once Bevy's task-pool runs systems that can panic. **Resolution path:** at the first M2/Epic 6+ story that adds threaded systems where worker panics are realistic, refactor `init_logging` to share `Arc<Mutex<File>>` between fmt-layer and the panic hook (or switch to `tracing-appender::non_blocking` with a flush guard). Source: Blind Hunter BH#1 + Edge Case Hunter EC#10 (overlap).
- **`catch_unwind` around `prev(info)` in panic hook** — `src/logging.rs:101`. If `prev` (the captured default hook) or our `writeln!` panics during execution of the hook itself, the process double-panics and aborts → no stderr backtrace shown. Tiny chance, real consequence. Standard panic-hook patterns don't guard this. **Resolution path:** revisit if a crash report shows corrupted hook output; wrap body in `std::panic::catch_unwind` to ensure `prev(info)` always runs. Source: Blind Hunter BH#4 + Edge Case Hunter EC#7.
- **Install panic hook before subscriber init** — `src/logging.rs:78-79`. Microsecond window between `tracing_subscriber::registry().init()` and `install_panic_hook(path.clone())` — a panic in that gap wouldn't reach the file. The hook writes to the file directly (not via subscriber), so order can swap. **Resolution path:** swap order in a future logging refactor. Practically negligible window today. Source: Blind Hunter BH#16 + Edge Case Hunter EC#22.
- **Cap backtrace output size for `RUST_BACKTRACE=full`** — `src/logging.rs:97`. A single panic with `RUST_BACKTRACE=full` could append multi-MB to the log file (deeply nested generic callees in Bevy / wgpu). Opt-in env var means the user explicitly wants verbose, so unlikely surprise. **Resolution path:** if a playtester reports balloon log files, add `backtrace.to_string().chars().take(N).collect::<String>()` truncation. Source: Edge Case Hunter EC#25.
- **Per-layer `EnvFilter` so stderr / file verbosity can diverge** — `src/logging.rs:69-77`. Today a single `EnvFilter` applies to both layers. Cannot capture verbose file logs while keeping stderr quiet during gameplay. **Resolution path:** introduce per-layer `.with_filter(EnvFilter::new(...))` if/when divergent filtering becomes wanted (e.g., capture per-frame `debug!` to file but keep stderr at info during playtests). Not required by any AC today. Source: Edge Case Hunter EC#5.
- **`eprintln!` log-init failures invisible in launchd / macOS app-bundle launches** — `src/logging.rs:54-77`. Pre-subscriber failures emit `eprintln!`, which lands at `/dev/null` when the binary is launched via macOS Launch Services / launchd / the future M9 `.app` bundle. **Resolution path:** at M9 packaging (Story 7.6 / 4.10 macOS universal binary), route pre-subscriber diagnostics to a fallback bootstrap log (e.g., `~/Library/Logs/asteroids3D/init.log`) or buffer + replay via `tracing::warn!` post-init. Not M0 scope. Source: Edge Case Hunter EC#23.
- **Validate `state_dir()` is absolute on Linux** — `src/logging.rs:34-37`. If `XDG_STATE_HOME` is set to a relative path or empty string, `create_dir_all` runs in CWD, polluting the project root. Rare misconfiguration. **Resolution path:** add `if state.is_absolute() { ... } else { fall through to home_dir-based default }`. Defer until first user reports it. Source: Edge Case Hunter EC#16.

## Deferred from: code review of 2-2-semanticaccent-palette-primitives (2026-04-28)

- **No despawn-on-state-exit for swatch UI; Camera2d (order:1) + Node tree will leak when state leaves `MainMenu`** — `src/visual/reference_scene.rs:118-200`. The new `spawn_palette_swatches` system spawns a dedicated `Camera2d { order: 1 }` plus a 5-swatch `Node` tree on `OnEnter(GameState::MainMenu)`, all tagged `ReferenceSceneEntity`. Story 2.2 deliberately ships no cleanup system — spec line 532 explicitly defers cleanup to Story 3.1 (Arena entry), which will despawn the entire dev scaffold (Camera3d + 3 meshes + 3 lights + swatch Camera2d + swatch Nodes + Text labels) in a single `Query<Entity, With<ReferenceSceneEntity>>` query. **No current re-entry path exists** — the M0/M1 flow is unidirectional `Loading → MainMenu → Arena`, and the swatches die at Arena entry. **Resolution path:** Story 3.1's despawn system handles this — verify at 3.1 review that swatch Camera2d + UI Nodes are caught by the cleanup query. **Future re-entry hazard (Story 4.7+ "back to main menu" flow):** if a future story introduces `Arena → MainMenu` re-entry, `spawn_palette_swatches` would re-fire and spawn duplicate Camera2d at the same `order: 1`, producing Bevy's "Camera order ambiguous" runtime warning per frame plus visible double-stacked UI. By the time 4.7 lands, Story 3.1 will have removed the swatches entirely (it's a tech-spike artifact), so this hazard self-resolves — but flagged here in case the swatch UI gets retained beyond M1. Source: code-review Blind Hunter + Edge Case Hunter (overlap, 2 layers). [src/visual/reference_scene.rs:118]
- **Over-tagging swatch children with `ReferenceSceneEntity`** — `src/visual/reference_scene.rs:154,167,177,186`. Story 2.2 honors spec line 103's "every dev-scaffold entity carries the marker" invariant by tagging the column wrapper Nodes, the swatch panel Nodes, and the Text labels — not just the root Node + Camera2d. Bevy 0.18's `despawn` is recursive by default; if Story 3.1's cleanup is the spec-prescribed `for e in q.iter() { commands.entity(e).despawn(); }`, despawning a parent will recursively despawn its tagged children, and the loop will then attempt to despawn already-freed entities → "tried to despawn missing entity" warnings (Bevy 0.18 returns Result, no panic, but log noise). **Resolution path:** Story 3.1's cleanup query should filter to roots only via `Without<ChildOf>` (or equivalent), OR the over-tagging should be removed at 3.1 by retaining markers only on the Camera2d + root Node. Either approach honors spec line 103's invariant for the roots. Source: code-review Blind Hunter + Edge Case Hunter (overlap, 2 layers). [src/visual/reference_scene.rs:154,167,177,186]
- **`review-notes.md` Notes column populated with placeholder ellipses across all 30 pair-rows** — `docs/tech-spike/m1-palette/review-notes.md:17-26, 32-41, 47-56`. The Wong (2011) palette is research-grounded and the global "GO — palette accepted as-is" verdict is plausible, but per-pair qualitative observations were not captured (every cell shows `…`). **Resolution path:** if a future colorblind-related design question surfaces (e.g. a player report, an accessibility audit, or a Story 2.3 toon-shader interaction that changes effective hue), revisit the screenshots and thicken the per-pair notes. Not actionable today — the auditable artifact (4 PNGs + GO conclusion) is the minimum AC #3 requires. Source: code-review Blind Hunter + Acceptance Auditor (overlap, 2 layers). [docs/tech-spike/m1-palette/review-notes.md]
- **Swatch UI bar overflows on viewports under ~416px width or ~525px height** — `src/visual/reference_scene.rs:130-150,161-165`. Bar height is `Val::Percent(12.0)`, swatch panels are `Val::Px(40.0)` fixed; on viewports shorter than ~333px the panels exceed the bar; with the 16px label below + 4px row gap + 8px padding, overflow starts at ~525px viewport height. The 5-swatch row is 5 × 80px = 400px fixed-width content + padding; viewports narrower than ~416px clip swatches because `JustifyContent::SpaceEvenly` doesn't shrink fixed-`Px` children. Concrete failure: during macOS resize-from-corner, swatches escape the dark bar mid-resize. **Resolution path:** dev tool, acceptable to break on extreme viewports. If real pain emerges (e.g. running on Steam Deck's 800×1280 at handheld scale), add an `overflow: clip` on the bar or switch swatch dimensions to percent-based. Story 3.1 removes the swatches entirely, so no real-game impact. Source: code-review Edge Case Hunter. [src/visual/reference_scene.rs:130]
- **Spec line 122 (`nm color_for >= 1` in release) was unsatisfiable as written for Story 2.2 in isolation** — `_bmad-output/implementation-artifacts/2-2-semanticaccent-palette-primitives.md:122`. The Task 4 sub-bullet asserts "the palette function IS in release because it's gameplay code (Story 2.3 toon material consumes it)." But Story 2.3 hasn't landed yet — `color_for` has no non-debug consumer in 2.2 isolation, so DCE drops the symbol from the release binary (`nm | grep -c color_for` returns 0, not the spec-expected ≥1). The implementer correctly preserved the architectural intent (cfg-gated dev tooling vs. release gameplay code) via `#[cfg_attr(not(debug_assertions), allow(dead_code))]` on the palette items; the spec line 122 expectation should be retroactively amended to "≥1 from Story 2.3 onward; 0 in 2.2 isolation." **Resolution path:** mention in Story 2.3 prep / spec amendment. Verify at 2.3 review that `color_for` reappears in the release binary (toon material consumes it server-side). Source: code-review Acceptance Auditor. [_bmad-output/implementation-artifacts/2-2-semanticaccent-palette-primitives.md:122]
- **No runtime integration test for `spawn_palette_swatches`** — `src/visual/reference_scene.rs:118-200`. There is no `App::new() → init_state → transition to MainMenu → assert swatch entities exist` integration test. Regressions like "swatch Camera2d removed accidentally" or "BackgroundColor wired to wrong field" produce no test failure; only manual screenshot review catches them. **Pre-existing pattern** — the reference_scene.rs Story 2.1 system has the same gap; architecture.md:354 explicitly defers integration tests post-M3. **Resolution path:** the first M3+ story that introduces ECS-spawn integration tests (per architecture.md:354) should add a swatch-spawn assertion alongside the reference-scene assertions. Source: code-review Edge Case Hunter. [src/visual/reference_scene.rs:118]

**Removal-on-graduation:** the `#[cfg_attr(not(debug_assertions), allow(dead_code, reason = "..."))]` annotations on `SemanticAccent` (palette.rs:7-13) and `color_for` (palette.rs:23-29) should be removed when Story 2.3 (toon material) lands a non-debug consumer of the palette. The annotation suppresses a release-build dead-code warning that exists *only* because the cfg(debug_assertions)-gated `mod reference_scene` is the sole 2.2 consumer; once 2.3 adds a release-path consumer, both items are reachable in release and the annotation becomes legitimate dead code itself. **Resolution path:** Story 2.3 dev should: (a) remove the two `cfg_attr` blocks from palette.rs, (b) verify `cargo build --release` is still 0-warnings, (c) verify `nm target/release/asteroids3D | grep -c color_for` is now ≥1. Source: Story 2.2 Dev Agent Record Completion Note 1 — promised follow-up that was missing from the deferred-work tracker until the 2.2 code review surfaced the gap. [src/visual/palette.rs:7-13,23-29]
