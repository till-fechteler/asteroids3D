# Deferred Work

Cross-story backlog of review findings intentionally deferred. Each entry lists the source review, original severity, and the reason for deferral so future sessions can re-evaluate.

## Deferred from: code review of 1-1-bootstrap-cargo-project-with-hand-authored-cargo-toml (2026-04-22)

- **`[package].rust-version` MSRV field not set** — Bevy 0.18 / `bevy_egui` 0.39 require rustc 1.89. Deferred to Story 1.3 (rust-toolchain.toml authoring) where MSRV metadata naturally lives.
- **`[profile.dev.build-override] opt-level = 0` not added** — Bevy's fully-recommended dev-profile snippet includes both `build-override` and `package."*"`; only the latter is in the current skeleton. Small compile-speed win; revisit in Story 1.3 toolchain polish or at an M4/M6/M9 upgrade window. **Re-deferred by Story 1.3 (2026-04-23)** to the M4 upgrade window: the compile-speed delta is imperceptible at the current single-`fn-main` codebase size, and bundling `Cargo.toml` profile changes with the next Bevy-version bump keeps manifest churn concentrated at governance-approved windows.
- **`default-features = false` may omit winit/wgpu-Metal on Windows/macOS** — Bevy 0.18's `"3d"` feature collection should transitively pull the needed renderer/windowing crates; this will be empirically validated when Story 1.5 opens the first window on each platform. If Story 1.5 surfaces a missing-feature error, patch back into Cargo.toml.
- **No explicit `[[bin]] name = "asteroids3d"` (lowercase)** — Binary inherits mixed-case package name (`asteroids3D`). Spec explicitly routes release-binary naming to Story 4.10 / Epic 10 packaging work. Leave as-is for now.

## Deferred from: code review of 1-3-toolchain-lint-and-format-configuration (2026-04-23)

- **MSRV `1.89` declared in `Cargo.toml` but never exercised by CI** — `rust-toolchain.toml` pins everyone to `1.94.1`. Promise to downstream consumers that the project builds on 1.89 is unverified. **Resolution path:** add a "msrv-check" job to Story 1.4's CI matrix running `cargo +1.89 check` on a single platform (Linux suffices). Low-severity today because no downstream consumer exists; becomes real the moment we publish to crates.io or accept external contributions.
- **`project: asteroids3D` typo (pre-existing) in `sprint-status.yaml`** — fixed in `Cargo.toml` by commit `113eebe` but still misspelled in all BMad artifacts (`sprint-status.yaml`, `architecture.md`, `prd.md`, `epic-1-*.md`, …). **Resolution path:** dedicated chore story "fix project-name typo across planning + bookkeeping artifacts" — rename wave, no code impact. Not 1.3 scope (1.3 only touches toolchain/lint/format).
- **`rust-toolchain.toml` lacks a `targets = [...]` field** — fine for one-native-runner-per-OS CI, breaks the moment anyone attempts cross-compilation. **Resolution path:** add to Story 1.4's Definition of Done — either (a) confirm CI uses native targets only, or (b) amend `rust-toolchain.toml` with explicit `targets`. No code change in 1.3.

## Deferred from: code review of 1-4-three-platform-ci-matrix (2026-04-23)

- **Third-party action pinning — SHA-pin vs tag/branch** — `dtolnay/rust-toolchain@master` (branch!) and `actions/checkout@v4` (moving major tag). GitHub security guide recommends full-SHA pinning for third-party actions. Revisit at M4/M6 upgrade window when Steam/Apple secrets may land in CI. Sources: Blind Hunter + Edge Case Hunter.
- **MSRV job lacks `--all-targets`** — `cargo check` without `--all-targets` skips tests/examples/benches on the Rust 1.89 leg. No-op today (zero tests); becomes real when Story 1.5+ adds tests. Fix: add `--all-targets` to `cargo check (MSRV)` step. Surface in the first test-adding story.
- **MSRV version hardcoded in `ci.yml` (two places); no dynamic sync with `Cargo.toml:5`** — If `rust-version` in `Cargo.toml` is bumped without editing `ci.yml`, MSRV job silently tests the obsolete pin. Fix options: (a) enforce dual-edit via upgrade-window checklist, (b) read MSRV dynamically via `cargo metadata`. MVP has ~3 MSRV bumps planned; option (a) is low-ceremony.
- **`cancel-in-progress: true` cancels in-flight `master` runs on rapid back-to-back pushes** — `.github/workflows/ci.yml:12`. Spec stated incorrectly that master has "unique refs" — all master pushes share `refs/heads/master`. Fix: condition on `github.ref != 'refs/heads/master'`. Low impact for solo-dev push cadence.
- **Same-repo PR double CI runs (push + pull_request events do not coalesce)** — `.github/workflows/ci.yml:6-13`. With both `push: branches: ["**"]` and `pull_request:`, a same-repo PR triggers two runs with different `github.ref` values (`refs/heads/feature` vs `refs/pull/N/merge`). Fix: restrict `push:` to `[master]` and let `pull_request:` carry feature-branch CI. Edge Case Hunter rated HIGH; Till-impact MED (rare PR flow in solo dev).
- **No `tags:` filter + future `release.yml` concurrency interaction risk** — `.github/workflows/ci.yml:7-8`. When Story 4.10 adds tag-triggered releases, concurrency won't coalesce. Surface in Story 4.10's design; not today's problem.
- **No retry wrapper around transient `apt-get update` failures** — `.github/workflows/ci.yml:34,77`. GitHub runner mirrors 503 occasionally; `&&` short-circuits → flaky red legs for non-code reasons. Mitigation: `for i in 1 2 3; do sudo apt-get update -y && break; sleep 5; done`.
- **Linux apt-deps duplicated across `build` and `msrv-check` jobs** — `.github/workflows/ci.yml:30-41,73-83`. Any dep addition must land in two places; drift risk. Fix: extract to composite action `.github/actions/install-linux-deps/` or YAML anchor. Acceptable for 2 copies; revisit at 3+ jobs.
- **`.gitattributes` lacks explicit binary markers for incoming assets** — `.gitattributes:2`. `* text=auto eol=lf` relies on Git's heuristic for `.png`, `.blend`, `.gltf`, `.ogg` (land in Story 2.1+). Pre-declare `*.png binary` etc. before first asset commit to eliminate heuristic risk.
- **`--workspace` not on clippy; future workspace-split gap** — `.github/workflows/ci.yml:59`. Single-crate today; a workspace split (architecture.md:130-131 permits post-M3) would make `clippy --all-targets` lint only the root crate. Surface when workspace split is considered.

## Review correction (2026-04-22) — formerly dismissed finding now confirmed real

> **✅ RESOLVED 2026-04-23 by Story 1.5** — removed `bevy_egui` and its `[target.'cfg(debug_assertions)'.dependencies]` host block from `Cargo.toml` (commit `03eb7a4`). Manifest warning cross-platform eliminated: CI run `24842252974` full-log grep → 0 `warning:` / 0 `error:` across 3542 lines (vs. 3× in Story 1.4's logs). Re-introduction planned at the M2 debug-panels story as an optional feature-flag dep (`[features] dev-tools = ["dep:bevy_egui"]` with `optional = true`), per `docs/plugin-compatibility.md` → Deferred / Planned re-introduction. Historical context preserved below for audit trail.

- **🐛 `[target.'cfg(debug_assertions)'.dependencies]` does NOT strip `bevy_egui` from release builds** — Cargo emits this warning on `cargo check`:
  `warning: Found 'debug_assertions' in target.'cfg(...)'.dependencies. This value is not supported for selecting dependencies and will not work as expected.`
  Cargo evaluates `cfg()` predicates in dependency tables BEFORE rustc-flag processing, so `debug_assertions` is treated as always-true and `bevy_egui` is pulled into every build (debug AND release). The Blind Hunter and Edge Case Hunter both raised this in the original 2026-04-22 review; both findings were incorrectly dismissed under the assumption that the cargo compile success implied correct semantics. The compile DID succeed — but the design intent ("dev-only egui, stripped from release") is silently broken.
  **Source of error:** the `Cargo.toml Skeleton` in story 1.1's Dev Notes (and presumably the upstream `architecture.md`) prescribes this exact (broken) pattern. Architecture doc may need an erratum.
  **Candidate fixes (architectural, NOT a Story 1.1 micro-patch):**
  1. Move `bevy_egui` to `[features] dev-tools = ["dep:bevy_egui"]` and run dev with `cargo run --features dev-tools`. Clean, explicit, but requires a features-section convention.
  2. Move `bevy_egui` to `[dev-dependencies]`. Only available in tests/examples — does NOT solve the "I want it in `cargo run` debug" use case.
  3. Keep `bevy_egui` as an unconditional dep, gate USAGE in Rust code with `#[cfg(debug_assertions)]`. Egui still in Cargo.lock and compile graph for release (binary bloat persists), but at least no runtime overhead.
  Recommended: option 1 (feature flag) — but defer to Story 1.5 (first time the egui plugin actually gets registered) so the fix and the first usage land together.

## Deferred from: code review of 1-5-minimal-bevy-app-opens-a-window-on-all-three-platforms (2026-04-23)

- **Historical doc-drift: planning artifacts still list `bevy_egui` as a current pinned dep** — hits in `_bmad-output/planning-artifacts/epics/epic-1-foundation-plugin-compatibility-gate.md:22,42`; `_bmad-output/planning-artifacts/epics.md:128,177,282,451,471`; `_bmad-output/planning-artifacts/epics/epic-list.md:5`; `_bmad-output/planning-artifacts/epics/requirements-inventory.md:108,157`. These are historical Story 1.1/1.2 ACs that were accurate at their time. **Resolution path:** dedicated doc-sync chore story (or append 2026-04-23 erratum pointers at the next planning-sweep story). Rewriting historical ACs in-place is rewriting history; a pointer-to-current-truth is safer. Source: code-review Edge Case Hunter EC3–EC6.
- **`architecture.md` prescribes the broken `cfg(debug_assertions)` pattern + shows `bevy_egui` in the starter Cargo.toml skeleton** — `_bmad-output/planning-artifacts/architecture.md:256,977`. Explicitly M2-owned per Story 1.5's Architecture Compliance section — the architecture erratum lands with the M2 debug-panels story alongside the first actual feature-flag usage. Flagged so a future M2 implementer does not copy the broken pattern. Source: code-review Edge Case Hunter EC7.
- **Story 1.2 artifact still shows `bevy_egui` in its gated-plugins matrix + `cfg(debug_assertions)` Known Issue** — `_bmad-output/implementation-artifacts/1-2-plugin-compatibility-verification-gate.md:72,113,123,156,210`. **Resolution path:** append a 2026-04-23 addendum noting removal by Story 1.5; leave historical body intact. Source: code-review Edge Case Hunter EC8.
- **`App::run()` return value (`AppExit`) is discarded by `fn main()` in `src/main.rs:7`** — Bevy 0.18's `App::run() -> AppExit` carries a crash-signal (`AppExit::Error(_)`) that is silently swallowed when `fn main()` returns `()`. Clippy did not flag it under current config. **Resolution path:** land the `fn main() -> AppExit { App::new()…run() }` pattern when `main.rs` is next touched — natural landing point is Story 1.6 (GameState wiring). Source: code-review Blind Hunter BH14 + Edge Case Hunter EC1.

  > **✅ RESOLVED 2026-04-24 by Story 1.6** — `fn main() -> AppExit` landed in `src/main.rs` (commit `19ed03c`) alongside the GameState wiring. `App::run()`'s return value is now propagated via the missing trailing `;`. CI run `24880100462` full-log grep: 0 `warning:` / 0 `error:` across 2059 lines on 3 OSes + MSRV. Bundled with 1.6 per deferred-work.md's explicit assignment (natural landing point: next story touching `main.rs`).
- **`.claude/scheduled_tasks.lock` runtime artifact is not gitignored** — appeared in the working tree during a Claude Code `ScheduleWakeup` call. `.gitignore` excludes `.claude/settings.local.json` (same category) but not arbitrary runtime state. **Resolution path:** add `.claude/*.lock` (or the specific filename) at the next `.gitignore`-touching story. Story 1.5's Task 7 scope guardrails explicitly forbid touching `.gitignore`. Source: code-review / dev-story verification.

## Deferred from: code review of 1-6-gamestate-enum-with-bevy-states-skeleton (2026-04-24)

- **`bevy_winit` emits `WARN Skipped event Destroyed for unknown winit Window Id` on window close** — reliably observed on macOS during `cargo run` close in Story 1.6 (`/tmp/story-1-6-run.log`). Known Bevy 0.18 winit-event race between `WindowCloseRequested` and `Destroyed`; not Story-1.6-introduced and not reproducible in CI (no interactive close). **Resolution path:** re-evaluate at Bevy version-bump window (M4). If still present post-bump, file upstream issue or gate via custom `terminal_ctrl_c_handler` replacement. Flagged now so future story Debug Logs don't treat this WARN as a regression signal. Source: code-review light-pass LOW-1 (Story 1.6).

## Deferred from: code review of 1-7-splash-screen-shows-asteroids3d-and-transitions-to-mainmenu (2026-04-24)

- **`SplashConfig.timer` latches after first fire; no re-entry reset path** — `TimerMode::Once` stays at "finished" after `just_finished()` fires once. If a future story re-enters `GameState::Loading` (e.g., restart-to-loading on error, dev hot-reload, or a meta-currency-earned-cutscene flow), `tick_splash_timer` would never re-fire `just_finished()` → state machine hangs in Loading. **No current re-entry path exists** in M0/M1 (the flow is unidirectional Loading → MainMenu), so not a Story 1.7 regression. **Resolution path:** the first future story introducing a Loading-re-entry path must either (a) add a reset system on `OnEnter(GameState::Loading)` that does `config.timer.reset()`, or (b) recreate the SplashConfig resource on entry. Flagged now so the land-mine is visible to that story's author. Source: code-review light-pass LOW defer (Story 1.7). [src/splash.rs:12-21]
